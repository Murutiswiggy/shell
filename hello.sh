#!/bin/bash

name="Maruti"
echo "${name}"

course="linux shell scripting"
echo "I am learing ${course}"

today=$(date)
echo "${today}"

user=$(whoami)
echo "current user is ${user}"

a=10
b=10
sum=$((a+b))
echo "total is ${sum}"
